export const STATE_DIR = ".envkit";
export const DIAGNOSTICS_DIR = `${STATE_DIR}/diagnostics`;
export const SNAPSHOT_FILE = `${STATE_DIR}/envkit.lock.json`;
export const LAST_APPLIED_FILE = `${STATE_DIR}/last-applied.json`;
export const DOCTOR_REPORT_FILE = `${DIAGNOSTICS_DIR}/doctor.json`;
export const LOG_FILE = `${STATE_DIR}/envkit.log`;
